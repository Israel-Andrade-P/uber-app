#application
spring:
  application:
    name: location-service
server:
  port: 8082
#redis
  data:
    redis:
      host: localhost
      port: 6379
#actuator
management:
  endpoints:
    web:
      exposure:
        include: health, info